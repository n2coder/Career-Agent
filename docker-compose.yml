services:
  career-agent-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: career-agent-api
    ports:
      - "8001:8001"
    env_file:
      - .env
    restart: unless-stopped
